# PumpkinCore

## Install
- Put `PumpkinCore-1.0.jar` in your server `plugins/` folder.
- Restart the server (or reload if you know it’s safe for your setup).

## Notes
- Bukkit `api-version`: `1.20`
