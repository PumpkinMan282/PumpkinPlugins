# PumpkinAPI

## Dependency
- Requires `PumpkinCore` (declared in `plugin.yml` as `depend`).

## Install
- Put `PumpkinCore-1.0.jar` and `PumpkinAPI-1.0.jar` in your server `plugins/` folder.
- Restart the server.
